#!/usr/bin/python3

def number_keys(a_dictionary):
    """Function to count number of keys of a given dictionary

    Args:
        a_dictionary: Dictionary to count keys of

    Returns:
        An integer. The length of the keys
    """
    return len(list(a_dictionary))
